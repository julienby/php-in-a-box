# Configuration du projet PHP-in-a-Box
# Copiez ce fichier en .env et personnalisez les valeurs

# Nom du projet (utilisé pour les noms d'images et conteneurs)
PROJECT_NAME=php-in-a-box

# Nom de l'image Docker
DOCKER_IMAGE_NAME=${PROJECT_NAME}-image

# Nom du conteneur Docker
DOCKER_CONTAINER_NAME=${PROJECT_NAME}-container

# Port d'exposition (modifiez si nécessaire)
EXPOSE_PORT=7080

# Version de PHP (optionnel, pour référence)
PHP_VERSION=8.2

# Variables d'environnement PHP
PHP_MEMORY_LIMIT=256M
PHP_MAX_EXECUTION_TIME=300
PHP_POST_MAX_SIZE=100M
PHP_UPLOAD_MAX_FILESIZE=100M

# Configuration de la base de données
DB_TYPE=sqlite
DB_NAME=database.sqlite

# Configuration du développement
DEBUG_MODE=true
DISPLAY_ERRORS=true
